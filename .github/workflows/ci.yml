name: CI

on:
  push:
  pull_request:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package
        run: python -m pip install -e .

      - name: Compile source
        run: python -m compileall -q src tests scripts

      - name: Privacy scan (tracked files, fail closed)
        run: python scripts/privacy_scan.py

      - name: Run privacy scan tests
        run: python tests/test_privacy_scan.py

      - name: Run private-data boundary tests
        run: python tests/test_private_data_boundary.py

      - name: Run synthetic end-to-end test
        run: python tests/test_synthetic_end_to_end.py
